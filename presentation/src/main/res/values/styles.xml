<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/beer_dark</item>
        <item name="colorPrimaryDark">@color/beer_dark</item>
        <item name="colorAccent">@color/beer_light</item>
    </style>

</resources>
